# 2026-08-04 开发日志

## 当日目标

从零建立 ESP32-S3 的 ESP-IDF 开发环境。

## 已完成

- 在 macOS 上安装 ESP-IDF Installation Manager（EIM）。
- 确认 EIM 安装成功，并获得独立的 IDF 终端环境。
- 核对 Python 环境；系统同时存在 Python 3.9.6 和 Homebrew Python 3.14.6，EIM 使用兼容的独立虚拟环境。
- 说明 OpenJDK 是安装过程中的依赖，不是 AmbientDesk 固件编译的核心工具。
- 熟悉 IDF 终端的用途：加载 ESP-IDF 工具链、Python 虚拟环境和 `idf.py` 命令。
- 确认 `cd <project-root>` 无输出代表切换目录成功。

## 当日问题与处理

- Homebrew 首次拒绝安装来自 `espressif/eim` 的 cask；按 Homebrew 提示执行信任操作后完成安装。
- 全局 `python3` 指向 3.14.6，但没有强制修改系统 Python，后续依赖 EIM 自己维护的环境。

## 遗留事项

- 创建并验证最小 ESP-IDF 工程。
- 连接 ESP32-S3 N16R8 Nano，完成首次编译、烧录和启动日志检查。
