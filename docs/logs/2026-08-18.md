# 2026-08-18 开发日志

## 当日目标

建立本地语音服务，并让 ESP32 与 DeepSeek 完成最小联网调用。

## 已完成

- 在 `server/` 建立 Python 虚拟环境和 FastAPI/Uvicorn 服务。
- 使用 `.env` 加载云服务配置，密钥未写入固件。
- 独立验证 DeepSeek 文本请求。
- 建立 `/health` 和 `/chat` 接口。
- ESP32 成功访问局域网 FastAPI 服务。
- 屏幕接入 `THINKING/SPEAKING/ERROR` 状态覆盖层。
- 修复 HTTP 请求导致的 `main` 任务栈溢出问题。
- 完成3秒录音：16 kHz、16位、单声道，共 48000 个采样、96000 字节。
- ESP32 将原始 PCM 上传到服务端，服务端保存为 `recordings/latest.wav`。
- 录音文件可以在本地电脑上检查和播放。

## 当日问题与处理

- 初始接口返回 HTTP 404，原因是服务端尚未实现对应录音路由；增加路由后恢复。
- 固定文本聊天与录音测试最初按启动顺序串行执行，等待时间较长，后续需要整合为按键触发的真实流程。

## 遗留事项

- 为录音接入真实 ASR。
- 接入功放与 TTS，使 AI 回答能从设备播放。
