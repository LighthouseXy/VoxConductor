# 2026-08-27｜上下文验证与展示证据整理

## 今日目标

- 将服务端单轮输出整理为适合调试与演示的结构化摘要。
- 用可重复的四轮语音场景验证会话上下文的读取、更新和再次引用。
- 将实机验证截图纳入公开文档，形成与 README 功能描述对应的证据。

## 已完成

- 服务端按轮次显示用户文本、助手回答、既有上下文轮数和处理结果。
- 每轮显示请求接收、ASR、模型、TTS 首包、完整音频时长和音频块数，便于定位延迟所在阶段。
- 完成连续四轮语音测试：
  1. 建立项目代号与 ESP32-S3 主控信息；
  2. 追问并正确引用主控；
  3. 修改项目代号；
  4. 同时回忆更新后的代号与原有主控。
- 两张测试截图保存到 `docs/images/context-dialog-turns-1-2.png` 和 `docs/images/context-dialog-turns-3-4.png`。
- README 与验收文档同步增加上下文测试说明和证据链接。

## 结论与边界

- 本次结果证明固定 `session_id` 下的短期多轮上下文能够读取和更新。
- 截图中的耗时来自一次实机测试，只用于展示链路阶段和定位延迟，不代表平均值、P95 或稳定性结论。
- 当前上下文仍是服务端 SQLite 中最近六轮消息，不等同于长期记忆，也尚未实现多用户或多设备隔离。
