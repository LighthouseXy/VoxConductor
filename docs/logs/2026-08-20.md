# 2026-08-20 开发日志

## 当日目标

复核前一日成果，并为语音延迟优化建立依据。

## 已完成

- 只读核对固件、服务器、录音/TTS 文件和构建产物。
- 确认前一日核心目标已完成：真实 ASR、DeepSeek、TTS、设备播放和按键语音闭环。
- 确认额外完成：语音状态 UI、重复请求保护、PSRAM 音频缓冲、启动测试关闭和 I2S 互斥修复。
- 确认 `.gitignore` 已排除 `.env`、Wi-Fi 凭据、构建目录、虚拟环境和 Python 缓存。
- 明确当前较长等待来自串行流程：固定3秒录音、ASR、DeepSeek、TTS、整段 PCM 下载。

## 待执行计划

- 在 `server/app.py` 使用 `time.perf_counter()` 分别统计 ASR、DeepSeek、TTS 和服务器总耗时。
- 连续测试三轮后，根据真实最慢阶段决定优化方向。
- 优先考虑缩短回答；只有 TTS 确认是主要瓶颈时再实现流式生成和播放。

## 当前未完成范围

- 正式语音按键和按住说话/松开提交。
- 播放取消、超时、`session_id/turn_id` 和迟到数据处理。
- microSD 文本会话记录与 AI 整理。
- HTTPS/WSS、断线恢复和连续多轮稳定性测试。
- BOM 与硬件总文档中的旧 `TBD/candidate` 状态尚未同步。
- Git 尚无正式提交；当前文件仍处于未跟踪状态。
