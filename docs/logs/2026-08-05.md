# 2026-08-05 开发日志

## 当日目标

建立可重复编译和烧录的最小固件工程，并准备首次接入 TFT。

## 已完成

- 在 `ambient_desk_firmware/` 建立最小 ESP-IDF 工程。
- 目标芯片设置为 ESP32-S3。
- Flash 容量设置为 16 MB。
- 完成最小工程的编译、烧录和启动日志验证。
- 明确 ESP-IDF 与 STM32 图形化引脚配置方式不同：ESP32 外设通常在代码和 `menuconfig` 中配置，并通过 GPIO Matrix 路由。
- 确认候选 TFT 的厂商标识：ST7789、SPI、240×240。
- 根据模块丝印确认屏幕接口为 `BLK/DC/RES/SDA/SCL/VCC/GND`，不假设模块存在 CS 引脚。
- 确定首轮保守接线：VCC 使用 3.3 V，BLK 在确认前不接。

## 接线记录

| TFT | ESP32-S3 Nano |
| --- | --- |
| VCC | 3V3 |
| GND | GND |
| DC | GPIO4 |
| RES | GPIO5 |
| SDA/MOSI | GPIO6 |
| SCL/SCLK | GPIO7 |
| BLK | 首轮不接，后续验证后接 3V3 |

## 遗留事项

- 使用 `esp_lcd` 验证 ST7789 初始化、颜色和显示偏移。
- 确认 BLK 电气要求并完成可见背光测试。
