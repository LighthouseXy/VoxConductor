# 2026-08-07 开发日志

## 当日目标

完成 ST7789 显示验证并接入 LVGL 9。

## 已完成

- 使用 ESP-IDF 原生 `esp_lcd` 驱动 ST7789 240×240 SPI 屏幕。
- 结合厂商资料修正初始化和显示偏移，解决局部异常画面。
- BLK 接入 3.3 V 后背光工作正常。
- 接入 LVGL 9 与 `esp_lvgl_port`，依赖重新配置成功。
- 屏幕显示 `Ambient Desk` 和 `LVGL9 OK`，确认 LVGL 刷新链路可用。
- 建立 HOME/INFO 页面和语音状态覆盖层的原型。
- 使用 BOOT 键完成临时页面切换与消抖测试。
- 项目目录已初始化 Git，但尚未建立正式提交。

## 工程说明

- VS Code 直接打开 `ambient_desk_firmware/` 时，ESP-IDF 扩展可识别根目录的 `CMakeLists.txt`。
- 打开上层 `ambient-desk/` 时，ESP-IDF 不会自动把子目录视作当前工程；编译终端需要进入固件目录。

## 遗留事项

- 接入麦克风和音频输出。
- 页面仍是功能验证界面，尚未完成正式视觉设计。
- BOOT 仅用于开发期测试，正式产品应使用普通 GPIO 按键。
