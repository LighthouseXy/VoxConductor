# 2026-08-17 开发日志

## 当日目标

扩展固件空间并接入基础联网功能。

## 已完成

- 发现 LVGL、字体、Wi-Fi 等组件加入后默认应用分区容量不足。
- 将 factory 应用分区扩展到 4 MiB，并完成重新编译和烧录。
- 确认开发板实际按 16 MB Flash 烧录。
- 接入 Wi-Fi STA 模式，成功取得局域网 IP。
- 加入本地 FastAPI `/health` 检查。
- 接入 SNTP，成功同步北京时间。
- HOME 页面显示当前时间，未同步时显示 `--:--:--`。
- 屏幕、LVGL 和麦克风在分区扩容后继续正常工作。

## 已验证现象

- Wi-Fi 日志出现连接成功和 DHCP 地址。
- 本地服务健康检查返回 HTTP 200。
- 串口输出正确的北京时间。

## 遗留事项

- Wi-Fi 地址和凭据仍为开发期本地配置。
- 尚未接入真正的语音服务链路。
